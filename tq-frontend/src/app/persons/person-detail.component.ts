import { Component } from '@angular/core';

@Component({
  selector: 'app-person-detail',
  template: `<h3>Person Detail Works!</h3>`
  // Remove "standalone: true" if present
})
export class PersonDetailComponent { }
